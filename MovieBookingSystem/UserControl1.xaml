<UserControl x:Class="MovieBookingSystem.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MovieBookingSystem"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#FF131314">
        <StackPanel>
            <TextBlock Text="Welcome to the Movie Booking System" 
                       FontSize="24" 
                       FontFamily="Poppins"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,10"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="MovieList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="Gray" 
                                    Background="White" CornerRadius="10"
                                    Width="150" Height="250" Margin="10" Padding="5">
                                <StackPanel>
                                    <Image Source="{Binding ImagePath}" 
                                           Height="200" 
                                           Stretch="Fill" 
                                           Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Title}" 
                                               TextAlignment="Center" 
                                               FontWeight="Bold" 
                                               Foreground="Black"/>
                                    <TextBlock Text="Hello World"
                                               TextAlignment="Justify"
                                               Foreground="White"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
